import styles from "./Menu.module.css";

const Menu = () => {
  return (
    <div className={styles.menu}>
      <div className={styles.menuChild}>
        <p>ALL CATEGORIES</p>
      </div>
    </div>
  );
};

export default Menu;
